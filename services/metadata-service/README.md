# metadata-service

Metadata service is the source-of-truth for document metadata, ACL, status, version pointers, and download authorization policy.

It no longer owns MinIO upload/download, workflow transitions, or audit persistence/query.
