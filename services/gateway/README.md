# gateway

Gateway verifies Keycloak JWT, applies route-level RBAC, forwards request/user context headers, and routes `/api/*` traffic to the downstream services.

See `../../README.md` for the full local run and demo flow.
