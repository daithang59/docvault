\chapter{Thiết kế dữ liệu, API và chính sách bảo mật}

\section{Tổng quan thiết kế dữ liệu}

DocVault tách dữ liệu thành ba nhóm chính. Metadata tài liệu thuộc metadata-service và lưu trong PostgreSQL. Nội dung file thuộc document-service và lưu trong MinIO/S3. Audit event thuộc audit-service và lưu trong MongoDB. Việc tách này giúp mỗi service sở hữu dữ liệu của mình, tránh việc service khác truy vấn trực tiếp vào database không thuộc bounded context của nó.

Tài liệu nội bộ \code{docs/ERD.md} mô tả rõ nguyên tắc này: metadata database lưu documents, document versions, ACL và workflow history; audit database lưu audit events; object storage lưu blob file. Nhờ tách như vậy, hệ thống truy vấn metadata nhanh, lưu được file dung lượng lớn trong object storage, đồng thời giữ audit log độc lập với nghiệp vụ tài liệu.

\begin{landscape}
\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth,height=0.86\textheight,keepaspectratio]{docvault-erd-visual-clean.drawio.png}
\caption{ERD và ranh giới sở hữu dữ liệu trong DocVault}
\label{fig:data-ownership-erd}
\end{figure}
\end{landscape}

DocVault tổ chức dữ liệu xoay quanh thực thể trung tâm \code{documents}, với các thực thể vệ tinh \code{document\_versions}, \code{document\_acl}, \code{document\_workflow\_history}, \code{document\_comments} và \code{document\_share\_links} cùng tham chiếu đến \code{documents} qua \code{docId} theo quan hệ một-nhiều (Hình~\ref{fig:data-ownership-erd}). Mỗi tài liệu có thể có nhiều phiên bản, nhiều rule ACL, nhiều bản ghi lịch sử workflow và nhiều comment.

Bên cạnh quan hệ thực thể, mô hình dữ liệu được phân chia theo ranh giới sở hữu tương ứng ba kho lưu trữ. Vùng PostgreSQL (thuộc metadata-service) gồm \code{documents} và các bảng vệ tinh, là nơi lưu metadata và quyền truy cập. Vùng object storage (thuộc document-service) lưu blob file; lưu ý \code{document\_versions} chỉ giữ \code{objectKey} và \code{checksum} trỏ tới file thật chứ không lưu nội dung. Vùng MongoDB (thuộc audit-service) lưu \code{audit\_events} và cố ý không dùng khóa ngoại cứng đến \code{documents}, vì audit là bounded context độc lập và phải tồn tại được kể cả khi tài liệu bị xóa. Đây cũng là lý do một thao tác như tải file phải đi qua nhiều service: metadata nằm một nơi, file nằm một nơi, và dấu vết kiểm toán nằm ở nơi thứ ba.

\section{Các thực thể chính}

\subsection{documents}

Bảng \code{documents} là nguồn sự thật cho tài liệu logic. Mỗi record đại diện cho một tài liệu, gồm tiêu đề, mô tả, owner, classification, tags, status, current version và các mốc thời gian như createdAt, updatedAt, publishedAt, archivedAt.

Trạng thái tài liệu gồm \code{DRAFT}, \code{PENDING}, \code{PUBLISHED} và \code{ARCHIVED}. Classification gồm \code{PUBLIC}, \code{INTERNAL}, \code{CONFIDENTIAL} và \code{SECRET}. Hai trường này là nền tảng cho phần lớn quyết định policy.

\subsection{document\_versions}

Bảng \code{document\_versions} lưu thông tin từng phiên bản file. Mỗi version có docId, version number, object key, checksum, size, filename, contentType và createdBy. File thật không nằm trong PostgreSQL; database chỉ giữ pointer đến object storage.

Checksum SHA-256 hỗ trợ kiểm tra tính nhất quán và làm evidence. Khi export evidence packet, hệ thống có thể chứng minh tài liệu có version cụ thể và checksum cụ thể mà không cần đưa nội dung file vào packet.

\subsection{document\_acl}

Bảng \code{document\_acl} lưu các rule truy cập theo tài liệu. Rule có subjectType, subjectId, permission và effect. SubjectType hỗ trợ \code{USER}, \code{ROLE}, \code{GROUP} và \code{ALL}. Permission hỗ trợ \code{READ}, \code{DOWNLOAD}, \code{WRITE} và \code{APPROVE}. Effect là \code{ALLOW} hoặc \code{DENY}.

ACL không thay thế RBAC mà bổ sung cho RBAC. RBAC xác định năng lực chung theo vai trò, trong khi ACL xác định ngoại lệ hoặc quyền cụ thể theo tài liệu. Khi có rule deny phù hợp, hệ thống ưu tiên từ chối.

\subsection{document\_workflow\_history}

Bảng \code{document\_workflow\_history} lưu lịch sử chuyển trạng thái nghiệp vụ. Mỗi record có docId, fromStatus, toStatus, action, actorId, reason và createdAt. Bảng này phục vụ truy vấn nhanh lịch sử phê duyệt trong màn hình chi tiết tài liệu, tách khỏi audit-service vốn ghi nhiều loại sự kiện hệ thống hơn.

\subsection{audit\_events}

Audit event ghi lại hành động trong hệ thống. Mỗi event chứa actor, action, resource type, resource id, result, reason, metadata và hash. Audit-service áp dụng hash-chain để phát hiện sửa đổi ngoài luồng. Vì audit là bounded context riêng, audit event không dùng foreign key cứng đến documents.

\section{Document lifecycle}
\label{sec:document-lifecycle}

Luồng trạng thái tài liệu trong DocVault được giữ đủ gọn để phù hợp với phạm vi MVP nhưng vẫn bao phủ demo doanh nghiệp. Các transition chính gồm tạo draft, gửi duyệt, phê duyệt, từ chối và lưu trữ; ngoài ra hệ thống còn mô phỏng soft delete và restore cho tài liệu nháp. Phần này đi sâu vào state machine; cách bốn vai trò tương tác với các trạng thái đó đã được trình bày ở Mục~\ref{sec:business-flow}.

\begin{landscape}
\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth,height=0.86\textheight,keepaspectratio]{docvault_document_lifecycle_state.drawio.png}
\caption{Vòng đời trạng thái tài liệu trong DocVault}
\label{fig:document-lifecycle}
\end{figure}
\end{landscape}

DocVault định nghĩa vòng đời tài liệu bằng một state machine với các trạng thái \code{DRAFT}, \code{PENDING}, \code{PUBLISHED}, \code{ARCHIVED}, \code{DELETED} và các transition có ràng buộc vai trò (Hình~\ref{fig:document-lifecycle}). Tài liệu mới luôn bắt đầu ở \code{DRAFT}. Editor thực hiện \code{SUBMIT} để chuyển sang \code{PENDING}; Approver thực hiện \code{APPROVE} để sang \code{PUBLISHED} hoặc \code{REJECT} để quay về \code{DRAFT}; tài liệu \code{PUBLISHED} có thể được \code{ARCHIVE}. Ngoài vòng đời chính, hệ thống còn hỗ trợ nhánh soft delete và restore áp dụng cho bản nháp.

Hai nguyên tắc chi phối vòng đời này. Thứ nhất, các transition là một chiều và ràng buộc theo vai trò, nên giữa hai trạng thái bất kỳ không có đường đi tùy ý; workflow-service kiểm tra transition có hợp lệ không trước khi yêu cầu metadata-service cập nhật. Thứ hai, mỗi transition sinh một bản ghi \code{document\_workflow\_history} kèm một audit event, đủ để trang chi tiết tài liệu dựng lại timeline nghiệp vụ và để compliance đối chiếu chéo với audit log. Khi tài liệu đi qua \code{APPROVE}, hệ thống đặt \code{publishedAt}; khi đi qua \code{ARCHIVE}, hệ thống đặt \code{archivedAt}.

\section{Ma trận phân quyền}

Policy của DocVault không chỉ dựa trên role. Kết quả truy cập được xác định bởi nhiều yếu tố: role, owner, ACL, classification, status, version existence và trạng thái deleted/archived. Tuy nhiên, ở mức tổng quan, ma trận dưới đây thể hiện ý nghĩa các vai trò chính.

\begin{table}[H]
\centering
\caption{Ma trận quyền tổng quan theo vai trò}
\label{tab:role-permission-summary}
\begin{tabularx}{\textwidth}{p{0.22\textwidth}Y}
\toprule
\textbf{Vai trò} & \textbf{Quyền chính} \\
\midrule
Viewer & Xem danh sách/metadata và preview/download tài liệu đã published trong phạm vi policy. \\
Editor & Tạo tài liệu, upload version, chỉnh metadata, submit, archive tài liệu thuộc quyền sở hữu hoặc được cấp quyền. \\
Approver & Xem hàng đợi duyệt, kiểm tra tài liệu pending, approve hoặc reject. \\
Compliance Officer & Xem audit, security summary, retention, evidence và metadata được phép; luôn bị chặn nội dung file. \\
Admin & Quản trị tài liệu, thành viên, evidence, retention và các thao tác nhạy cảm. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Quyền xem và tải theo classification}

Bảng~\ref{tab:content-access-by-role-classification} mô tả quyền truy cập nội dung file trong luồng người dùng thông thường. Ký hiệu \textbf{X} là xem trước nội dung và \textbf{T} là tải file. Quyền tải chỉ được xét khi tài liệu ở trạng thái \code{PUBLISHED} và đã có version; quyền xem trước có thể áp dụng ở các trạng thái workflow khác nếu đã có version. ACL \code{DENY} phù hợp được ưu tiên hơn quyền role. Các ô ghi ``Owner/ACL'' yêu cầu người dùng là chủ sở hữu hoặc có ACL \code{ALLOW} đúng loại quyền.

\begin{landscape}
\begin{table}[H]
\centering
\small
\caption{Quyền xem và tải nội dung theo vai trò và classification}
\label{tab:content-access-by-role-classification}
\begin{tabularx}{\linewidth}{p{0.16\linewidth}YYYY}
\toprule
\textbf{Vai trò} &
\textbf{PUBLIC} &
\textbf{INTERNAL} &
\textbf{CONFIDENTIAL} &
\textbf{SECRET} \\
\midrule
Viewer &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Không\newline \textbf{T:} Không &
\textbf{X:} Owner/ACL READ\newline \textbf{T:} Owner/ACL DOWNLOAD &
\textbf{X:} Chỉ owner\newline \textbf{T:} Không \\
\addlinespace
Editor &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Owner/ACL READ\newline \textbf{T:} Owner/ACL DOWNLOAD &
\textbf{X:} Chỉ owner\newline \textbf{T:} Không \\
\addlinespace
Approver &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Owner/ACL DOWNLOAD &
\textbf{X:} Có\newline \textbf{T:} Owner/ACL DOWNLOAD \\
\addlinespace
Compliance Officer &
\textbf{X:} Không\newline \textbf{T:} Không &
\textbf{X:} Không\newline \textbf{T:} Không &
\textbf{X:} Không\newline \textbf{T:} Không &
\textbf{X:} Không\newline \textbf{T:} Không \\
\addlinespace
Admin &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Có &
\textbf{X:} Có\newline \textbf{T:} Có \\
\bottomrule
\end{tabularx}
\end{table}
\end{landscape}

Compliance Officer vẫn có thể đọc metadata của tài liệu \code{PUBLISHED}/\code{ARCHIVED} để phục vụ audit và evidence, nhưng không có quyền xem trước, stream, presign hoặc tải nội dung file ở bất kỳ classification nào. Share link có grant hợp lệ là một cơ chế cấp quyền riêng và không được biểu diễn trong ma trận role cơ sở này.

\section{Chính sách Compliance Officer}

Một quyết định thiết kế quan trọng của DocVault là tách rõ quyền compliance khỏi quyền truy cập nội dung file. Compliance Officer cần xem metadata và audit để đánh giá tuân thủ, nhưng có quyền audit không đồng nghĩa với được tải file. Tách hai quyền này lại giảm rủi ro ai đó lạm dụng vai trò compliance để chạm tới tài liệu nhạy cảm.

Trong DocVault, Compliance Officer:

\begin{itemize}
  \item được truy vấn audit events;
  \item được verify audit hash-chain;
  \item được xem security summary;
  \item được xem retention evidence;
  \item được export evidence packet;
  \item không được preview, stream, presign hoặc download nội dung file.
\end{itemize}

Quy tắc này được enforce ở nhiều tầng: frontend permission helper để hiển thị lý do deny, gateway proxy để chặn đường gọi phù hợp và service policy để đảm bảo backend là nguồn quyết định cuối cùng.

\section{Thiết kế preview/download authorization}

Download được thiết kế thành luồng hai bước. Ở bước đầu, frontend gọi metadata-service thông qua gateway để xin authorize. Metadata-service kiểm tra policy và nếu hợp lệ sẽ cấp grant token ngắn hạn. Ở bước sau, frontend dùng grant token để gọi document-service. Document-service xác minh grant token hoặc tái authorize trước khi cấp presigned URL hoặc stream endpoint.

\begin{landscape}
\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth,height=0.88\textheight,keepaspectratio]{download-authorization-sequence.png}
\caption{Sequence cấp quyền download và stream file}
\label{fig:download-authorization}
\end{figure}
\end{landscape}

Với tài liệu \code{PUBLIC} hoặc \code{INTERNAL}, nếu policy cho phép, hệ thống có thể trả về presigned URL. Với tài liệu \code{CONFIDENTIAL} hoặc \code{SECRET}, hệ thống trả về \code{url: null}, \code{watermarkRequired: true} và \code{streamingEndpoint}, để document-service nắm được đường tải nội dung nhạy cảm và kiểm soát chặt hơn.

\section{Malware scanning và DLP}

Document-service thực hiện scanning trước khi ghi object vào MinIO. Với payload EICAR trong môi trường demo, upload bị chặn và không tạo object cũng như không đăng ký metadata version. Đây là một điểm quan trọng: nếu scan chỉ phát hiện sau khi file đã lưu, hệ thống sẽ phải xử lý cleanup phức tạp hơn.

DLP scan phát hiện một số pattern nhạy cảm như email, số điện thoại/số định danh và từ khóa như \code{secret}, \code{confidential}, \code{internal only}. Khi phát hiện dữ liệu nhạy cảm trong tài liệu public/internal, hệ thống nâng classification lên \code{CONFIDENTIAL}. Người dùng không có quyền không thể hạ classification xuống public/internal nếu không có lý do override hợp lệ.

\section{Audit ingest boundary}

Audit-service không cho người dùng thường append audit event trực tiếp. Service tin cậy phải gửi kèm header service token \code{x-docvault-service-token}, cấu hình qua \code{AUDIT\_INGEST\_TOKEN}. Rào này chặn việc một người có JWT hợp lệ tạo audit event giả để che giấu hoặc làm sai lệch lịch sử.

\section{Luồng compliance evidence}

Compliance evidence trong DocVault được xây dựng từ nhiều tín hiệu runtime: workflow event, access event, upload control, retention record và access review signal. Các tín hiệu này được audit-service ghi nhận theo dạng append-only và hash-chain, sau đó được các màn hình Security, Audit, Retention, Access Review và Evidence Center tổng hợp thành bằng chứng có thể xuất ra ngoài.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth,height=0.82\textheight,keepaspectratio]{docvault_compliance_evidence_flow.drawio.png}
\caption{Luồng tổng hợp compliance evidence trong DocVault}
\label{fig:compliance-evidence-flow}
\end{figure}

DocVault tổng hợp compliance evidence theo một luồng ba giai đoạn từ tín hiệu runtime đến bằng chứng có thể xuất ra ngoài (Hình~\ref{fig:compliance-evidence-flow}). Giai đoạn nguồn là các tín hiệu phát sinh trong vận hành: workflow event, access event, upload control (malware/DLP), retention record và access review signal. Giai đoạn lưu trữ là audit-service ghi nhận các tín hiệu này theo dạng append-only kèm hash-chain, tạo thành một dòng sự kiện có thể kiểm tra tính toàn vẹn. Giai đoạn tổng hợp là các màn hình Security, Audit, Retention, Access Review và Evidence Center đọc dòng sự kiện đó và kết tinh thành bằng chứng.

Luồng được thiết kế một chiều từ nguồn đến evidence, nên bằng chứng luôn dẫn xuất từ tín hiệu đã ghi chứ không phải dữ liệu nhập tay, khó bị làm giả sau sự việc. Ngay trước khi xuất, một lằn ranh lọc bảo đảm evidence packet không bao giờ chứa file content, object key, presigned URL hay grant token. Compliance Officer nhờ đó kiểm tra được tính tuân thủ và truy vết hoạt động, còn quyền của họ không bị nới rộng thành quyền đọc nội dung tài liệu.

\section{API contract tổng quan}

API của hệ thống được expose qua gateway với base path \code{/api}. Các nhóm endpoint chính gồm:

\begingroup
\small
\begin{longtable}{p{0.23\textwidth}p{0.34\textwidth}p{0.33\textwidth}}
\caption{Nhóm API chính của DocVault}
\label{tab:api-groups} \\
\toprule
\textbf{Nhóm API} & \textbf{Ví dụ endpoint} & \textbf{Mục đích} \\
\midrule
\endfirsthead
\toprule
\textbf{Nhóm API} & \textbf{Ví dụ endpoint} & \textbf{Mục đích} \\
\midrule
\endhead
Auth & \code{/auth/login}, \code{/auth/me}, \code{/auth/logout} & Đăng nhập, callback, logout và lấy người dùng hiện tại. \\
Metadata & \code{/metadata/documents}, \code{/metadata/documents/:id} & Tạo, đọc, cập nhật metadata tài liệu. \\
ACL & \codecell{\code{/metadata/documents/:id/}\\\code{acl}} & Thêm/xem rule ACL theo tài liệu. \\
Version & \codecell{\code{/metadata/documents/:id/}\\\code{versions}} & Đăng ký version metadata sau upload. \\
Download authorization & \codecell{\code{/metadata/documents/:id/}\\\code{download-authorize}} & Xin quyền download và grant token. \\
Preview authorization & \codecell{\code{/metadata/documents/:id/}\\\code{preview-authorize}} & Xin quyền preview nội dung file. \\
Document blob & \codecell{\code{/documents/:id/upload}\\\code{/documents/:id/}\\\code{presign-download}\\\code{/documents/:id/versions/}\\\code{:v/stream}} & Upload, presign hoặc stream file. \\
Workflow & \code{/workflow/:id/submit}, \code{/workflow/:id/approve} & Chuyển trạng thái tài liệu. \\
Audit & \code{/audit/query}, \code{/audit/verify-chain}, \code{/audit/security-summary} & Truy vấn audit và kiểm tra hash-chain. \\
Evidence & \codecell{\code{/metadata/documents/:id/}\\\code{evidence-packet}} & Xuất evidence packet metadata-only. \\
Retention & \codecell{\code{/metadata/retention/}\\\code{documents}\\\code{/metadata/retention/run}} & Xem và chạy retention demo. \\
\bottomrule
\end{longtable}
\endgroup
