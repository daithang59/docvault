\chapter{Danh mục hình ảnh báo cáo}

\section{Ảnh giao diện ứng dụng}

Các ảnh trong \code{report/images/screen} minh họa những chức năng chính của DocVault:

\begin{itemize}
  \item \code{loginpage.png}: trang đăng nhập ứng dụng DocVault.
  \item \code{authpage.png}: trang xác thực Keycloak.
  \item \code{keycloakconsole.png}: danh sách user trong realm Keycloak.
  \item \code{dashboard.png}: dashboard tổng quan.
  \item \code{document.png}: danh sách tài liệu.
  \item \code{mydocument.png}: danh sách tài liệu của người dùng hiện tại.
  \item \code{newdocument.png}: màn hình tạo tài liệu và upload file.
  \item \code{documentdetail.png}: chi tiết tài liệu, metadata và workflow.
  \item \code{preview-document.png}: preview nội dung tài liệu qua luồng kiểm soát.
  \item \code{approvals.png}: hàng đợi phê duyệt.
  \item \code{audit.png}: audit log.
  \item \code{security.png}: security posture.
  \item \code{evidencecenter.png}: Evidence Center.
  \item \code{retention.png}: quản lý retention và trạng thái lưu giữ.
  \item \code{accessreview.png}: rà soát quyền truy cập.
\end{itemize}

Các ảnh trên bao phủ các điểm chính của frontend: đăng nhập, xác thực, danh sách tài liệu, tạo tài liệu, chi tiết/preview, phê duyệt, audit, security posture, evidence, retention và access review.

\section{Ảnh DevSecOps đã sử dụng}

Các ảnh sau trong \code{report/images/infra} đã được chèn vào Chương 4:

\begin{itemize}
  \item \code{CICDPipelineFlow.png}: sơ đồ tổng quan CI, CD theo GitOps và kiểm tra sau triển khai.
  \item \code{docvault-eks-architecture.png}: kiến trúc triển khai DocVault trên EKS.
  \item \code{docvault-eks.png}: thông tin cụm EKS.
  \item \code{node-group.png}: managed node group và worker node.
  \item \code{docvault-eks-vpc.png}: VPC và các subnet.
  \item \code{s3bucket.png}: bucket lưu tài liệu và trạng thái versioning.
  \item \code{s3bucketkms.png}: cấu hình mã hóa SSE-KMS của bucket tài liệu.
  \item \code{kms.png}: các customer-managed KMS key.
  \item \code{iamroles.png}: IAM role dùng cho IRSA.
  \item \code{eks-workloads.png}: workload trên cụm EKS.
  \item \code{externalsecret.png}: danh sách secret trong AWS Secrets Manager.
  \item \code{argocd-application.png}: trạng thái application gốc và các application con.
  \item \code{harbor.png}: project và repository trên Harbor.
  \item \code{harbor-docvault.png}: digest, trạng thái ký và SBOM của artifact.
\end{itemize}

\section{Ảnh DevSecOps còn cần bổ sung}

Các placeholder còn lại trong Chương 4 tương ứng với các bằng chứng chưa có ảnh chụp:

\begin{itemize}
  \item \code{s3-backup.png}
  \item \code{jenkins-stage-view.png}
  \item \code{sonarqube-quality-gate.png}
  \item \code{sca-trivy-results.png}
  \item \code{iac-policy-results.png}
  \item \code{post-deploy-validation.png}
  \item \code{grafana-dashboard.png}
  \item \code{loki-logs.png}
\end{itemize}

Khi bổ sung ảnh, cần sử dụng cùng một lần phát hành đối với Jenkins, Harbor, GitOps và Argo CD để bảo đảm khả năng đối chiếu commit, digest và trạng thái triển khai.

\section{Yêu cầu bảo mật đối với ảnh}

Trước khi đưa ảnh vào báo cáo, phải loại bỏ hoặc che các thông tin sau:

\begin{itemize}
  \item password, token, secret value, private key và credential ID có tính nhạy cảm;
  \item nội dung kubeconfig, JWT, cookie, presigned URL và object key thực;
  \item dữ liệu tài liệu của người dùng;
  \item access key, secret key, session token hoặc giá trị secret trên AWS;
  \item địa chỉ quản trị nội bộ không phục vụ giải thích kiến trúc.
\end{itemize}
