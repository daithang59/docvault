'use client';

/**
 * @deprecated Import from `@/providers/auth-provider` instead.
 * This shim ensures Phase 1 components continue to work without modification.
 */
export { useAuth, AuthProvider } from '@/providers/auth-provider';
