# audit-service values
namespace: docvault
replicaCount: 1

image:
  repository: "harbor.docvault.id.vn/docvault-dev/audit-service"
  tag: "9c7b7077cf48"
  digest: "sha256:235a9c4c04822432bb114b3335f23caabfc4560b65bad3cac8b79b1bba8541ea"

healthPath: /health

service:
  type: ClusterIP
  port: 3004
env:
  NODE_ENV: production
  PORT: "3004"
  KEYCLOAK_BASE_URL: "http://keycloak:8080"
  KEYCLOAK_ISSUER: "https://auth.docvault.id.vn/realms/docvault"
  KEYCLOAK_REALM: "docvault"
  KEYCLOAK_AUDIENCE: "docvault-gateway"

envValueFrom:
  - name: MONGODB_URI
    valueFrom:
      secretKeyRef:
        name: audit-mongodb-app-credentials
        key: MONGODB_URI

envFrom:
  - secretRef:
      name: docvault-app-secrets
