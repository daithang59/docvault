PORT=3005
KEYCLOAK_BASE_URL=http://localhost:8080
KEYCLOAK_REALM=docvault
KEYCLOAK_AUDIENCE=docvault-gateway
KEYCLOAK_CLIENT_SECRET=dev-gateway-secret


# PostgreSQL — metadata-service database
DATABASE_URL=<postgres-connection-url>
# Optional: runtime connection as a non-superuser role so PostgreSQL RLS is
# enforced. Leave unset to use DATABASE_URL. See services/metadata-service/prisma/rls/README.md
DATABASE_URL_RUNTIME=<postgres-runtime-connection-url>

# MongoDB — audit-service database
MONGODB_URI=<mongodb-connection-url>

# S3 / MinIO — document-service
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=<s3-secret-key>
S3_BUCKET=docvault
S3_FORCE_PATH_STYLE=true

# Grant secrets (shared by metadata-service and document-service)
DOWNLOAD_GRANT_SECRET=docvault-download-grant-secret
PREVIEW_GRANT_SECRET=docvault-preview-grant-secret

# Keycloak admin credentials — used by metadata-service getApprovers()
# to query Keycloak Admin API for users with approver/admin roles.
# Required for SUBMIT notifications to reach approvers.
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=<keycloak-admin-password>

# Service URLs
METADATA_SERVICE_URL=http://localhost:3001
DOCUMENT_SERVICE_URL=http://localhost:3002
WORKFLOW_SERVICE_URL=http://localhost:3003
AUDIT_SERVICE_URL=http://localhost:3004
AUDIT_INGEST_TOKEN=replace-with-strong-internal-audit-ingest-token
NOTIFICATION_SERVICE_URL=http://localhost:3005

# Shared secret for trusted internal service-to-service calls (rate-limit exemption).
# Leave empty to disable the exemption entirely (fail-closed). A guessable flag
# is never enough — callers must send this value in the x-internal-call header.
INTERNAL_CALL_SECRET=

# Gateway
GATEWAY_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3006
