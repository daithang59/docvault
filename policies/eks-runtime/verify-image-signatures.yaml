apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: verify-image-signatures
  annotations:
    policies.kyverno.io/title: Verify DocVault Image Signatures
    policies.kyverno.io/description: >-
      Verifies that DocVault container images are signed by Cosign
      before permitting deployment.
spec:
  validationFailureAction: Enforce
  background: false
  webhookConfiguration:
    failurePolicy: Fail
  rules:
    - name: verify-cosign-signature
      skipBackgroundRequests: true
      match:
        any:
          - resources:
              kinds:
                - Pod
              selector:
                matchLabels:
                  app.kubernetes.io/part-of: docvault-app
      verifyImages:
        - imageReferences:
            - "harbor.docvault.id.vn/docvault-dev/*"
          mutateDigest: true
          required: true
          useCache: true
          verifyDigest: true
          attestors:
            - entries:
                - keys:
                    secret:
                      name: cosign-public-key
                      namespace: kyverno
                    signatureAlgorithm: sha256
                  signatureAlgorithm: sha256
